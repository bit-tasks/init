name: 'Initalize Bit'
description: 'Initialize Bit'
inputs:
  ws-dir:
    description: "Workspace json file directory path"
    required: false
    default: './'
runs:
  using: 'node16'
  main: 'index.js'
